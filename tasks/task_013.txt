# Task ID: 13
# Title: Integrate LiteLLM for Model Management
# Status: in-progress
# Dependencies: 1, 6
# Priority: high
# Description: Implement LiteLLM proxy for managing multiple LLM providers and local models.
# Details:
Set up LiteLLM proxy in docker-compose.yml. Configure support for multiple LLM providers (e.g., OpenAI, Anthropic, Cohere). Implement model routing based on request parameters. Set up load balancing for distributed inference. Integrate Ollama for local model support. Use litellm Python library for seamless integration with FastAPI backend.

# Test Strategy:
Test integration with different LLM providers. Verify model routing and load balancing functionality. Benchmark inference performance across different models. Test fallback mechanisms for provider outages.

# Subtasks:
## 1. LiteLLM Installation and Basic Setup [done]
### Dependencies: None
### Description: Install LiteLLM and set up the initial configuration for accessing multiple LLM providers
### Details:
Install LiteLLM using pip, set up environment variables for API keys (OpenAI, Anthropic, etc.), and create a basic config.yaml file structure for the LiteLLM proxy server

## 2. Provider Configuration [done]
### Dependencies: 13.1
### Description: Configure multiple LLM providers with their specific parameters
### Details:
Set up model-specific configurations in config.yaml including API bases, keys, temperatures, and other provider-specific parameters for services like OpenAI, Azure, and Anthropic
<info added on 2025-05-29T00:24:46.910Z>
Implemented advanced routing strategies for model management using LiteLLM's Router capabilities. Added weighted distribution across model deployments with the following configurations:

1. Set up usage-based routing for production environments with Redis integration to track TPM/RPM across deployments
2. Configured latency-based routing as fallback strategy to optimize response times
3. Implemented cost-based routing for budget-sensitive operations
4. Added retry mechanisms with configurable wait times (retry_after=5s) for handling failed requests
5. Created model groups with appropriate fallback chains:
   - Primary groups: fast, vision, local, code, embeddings, budget
   - Each group configured with multiple deployment options of the same model type
6. Documented provider-specific parameters including temperature settings, max tokens, and other tuning options
7. Added simple-shuffle strategy for development environments to balance load without Redis dependency
8. Implemented prompt caching optimization to reduce costs and latency by routing similar prompts to the same deployment

All configurations stored in config.yaml with environment variable references for sensitive credentials.
</info added on 2025-05-29T00:24:46.910Z>
<info added on 2025-05-29T00:28:49.168Z>
Enhanced the model configuration in config.yaml with comprehensive production-ready settings optimized for M4 Mac Mini deployment. Implemented advanced routing strategies including usage-based routing with Redis integration and fallback to simple-shuffle for development environments. Configured robust fallback chains based on capability, cost, and performance metrics with intelligent progression from Premium → Standard → Fast → Budget models. Set up optimized system parameters including 900s request timeout, 180s health check interval, and production-ready rate limits (1000 RPM, 150K TPM). Created specialized model groups organized by use case (quality, speed, cost, privacy) with local model fallback chain (llama3.2-1b → gpt-3.5-turbo → claude-3-haiku). Implemented comprehensive monitoring with Prometheus metrics integration, per-model/user/API-key cost tracking, and regular health checks. Added security enhancements including UI password protection, enhanced headers, and proper CORS configuration. Performance optimizations include response caching and weighted round-robin load balancing. All configurations successfully tested and verified working with 16 available models including local model chat completion functionality.
</info added on 2025-05-29T00:28:49.168Z>

## 3. Model Routing Logic Implementation [pending]
### Dependencies: 13.2
### Description: Implement logic to route requests to appropriate models based on criteria
### Details:
Create routing rules in the config to direct requests to specific models based on requirements, including fallback options and conditional routing based on request parameters

## 4. Load Balancing Configuration [pending]
### Dependencies: 13.3
### Description: Set up load balancing across multiple model instances
### Details:
Configure load balancing in LiteLLM to distribute requests across multiple instances of the same model or similar models, including setting weights and deployment strategies

## 5. Ollama Integration [pending]
### Dependencies: 13.2
### Description: Integrate Ollama for local model deployment
### Details:
Configure Ollama integration in the LiteLLM setup, including API base settings, model specifications for local models like Mistral-7B, and handling the connection between LiteLLM and the Ollama instance

## 6. Testing and Validation [pending]
### Dependencies: 13.3, 13.4, 13.5
### Description: Test the complete setup with various scenarios
### Details:
Create test cases to validate routing logic, load balancing, provider failover, and Ollama integration, including performance testing and error handling scenarios

